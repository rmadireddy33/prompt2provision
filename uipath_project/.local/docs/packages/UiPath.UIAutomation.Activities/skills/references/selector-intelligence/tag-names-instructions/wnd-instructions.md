## Wnd:

a) very reliable: app, cls
b) alright: title

Maintain at least part of `title` in <wnd> selectors.
Before wildcarding window attributes, scan the Window Level Context for other elements that would also match — if wildcarding causes collisions with other open windows, use a more specific attribute or tighter wildcard.